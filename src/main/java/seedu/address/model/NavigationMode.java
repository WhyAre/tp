package seedu.address.model;

/**
 * Specifies the current navigation mode to accommodate functionality and UI.
 */
public enum NavigationMode {
    STUDENT, TUTORIAL, ATTENDANCE, SUBMISSION, SINGLE_STUDENT, UNCHANGED
}
